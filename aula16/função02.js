function soma(n1=0, n2=0) { //Posso adicionar um "=0" na frente dos elementos para atribui-los um valor caso o mesmo não seja definido, no caso se eu não definir o valor, ele será "0".
    return n1 + n2
}

console.log(soma(7))